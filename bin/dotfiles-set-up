#!/bin/bash

cd

repo=.dotfiles.pub

for item in bin '.bash*' '.vim*' .tmux.conf .inputrc .gitconfig; do
    ln -vsf ~/$repo/$item ~/
done

ln -vsf ~/$repo/.lein/profiles.clj ~/.lein/

mkdir -p ~/.emacs.d
ln -vsf ~/$repo/.emacs.d/init.el ~/.emacs.d/

